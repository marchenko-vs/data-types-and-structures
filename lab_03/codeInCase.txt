// for (size_t i = 0; i < matrix_rows; i++) // РАБОЧЕЕ УМНОЖЕНИЕ РАЗР. МАТРИЦЫ НА ПЛОТНЫЙ ВЕКТОР
    // {
    //     double tmp_elem = 0;
    //     for (int j = sparse_martix.list[i] - 1; j < sparse_martix.list[i + 1] - 1; j++)
    //         tmp_elem += sparse_martix.values[j] * init_vector[sparse_martix.cols[j] - 1];
    //     res_vector[i] = tmp_elem;
    // }
    // print_result(res_vector, matrix_cols);